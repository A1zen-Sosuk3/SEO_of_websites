"use strict"

let typed = new Typed('#typed', {
    typeSpeed: 100,
    backSpeed: 50,
    startDelay: 500,
    loop: true,
    stringsElement: '#typed-strings'
});

VANTA.NET({
    el: "#wrapper",
    mouseControls: true,
    touchControls: true,
    gyroControls: false,
    minHeight: 200.00,
    minWidth: 200.00,
    scale: 1.00,
    scaleMobile: 1.00,
    backgroundColor: 0x0
});